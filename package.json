{
  "name": "gym-management",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "node build.js && npm install",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "fs-extra": "^11.3.0"
  }
}
